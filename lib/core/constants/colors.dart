import 'package:flutter/material.dart';

/// Clase que contiene la paleta de colores personalizada para la app.
/// Se usa para mantener la coherencia visual y facilitar cambios de color en un solo lugar.
class AppColors {
  static const primary = Color(0xFF1976D2); // Azul fuerte, color principal
  static const secondary = Color(0xFF42A5F5); // Azul claro, color secundario
  static const background = Color(
    0xFFF5F5F5,
  ); // Gris muy claro, fondo de pantallas
  static const text = Color(
    0xFF212121,
  ); // Gris muy oscuro, color principal de texto
}
